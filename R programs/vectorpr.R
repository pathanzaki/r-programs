num_vec <- c(1, 2, 3, 4.5)
int_vec <- c(1L, 2L, 3L)
char_vec <- c("apple", "banana", "cherry")
log_vec <- c(TRUE, FALSE, TRUE)

class(num_vec)     # "numeric"
typeof(int_vec)    # "integer"
length(char_vec)   # 3
str(log_vec)       # atomic vector of logicals
